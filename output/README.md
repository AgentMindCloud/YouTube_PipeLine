# output/

Created at runtime. Gitignored.

Each run is `output/<timestamp>-<slug>/` with `final.mp4`, `thumb.png`, `metadata.json`, `status.json`, voice/music/captions.

`last_ideas.json` is written by `python run.py idea` and read by `python run.py run --pick N`.
